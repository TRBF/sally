# Sally Citizen App

## Backend URL configuration

Create a `.env` file (or copy `.env.example`) and set:

```bash
EXPO_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
```

Change this value whenever backend host/IP changes.

## Run

```bash
npm install
npm run start
```
